# CI / unit-test dependencies — Django stack only (ML is mocked in tests).
# Keep Django-related pins aligned with requirements.txt.
#
#   pip install -r requirements-ci.txt

Django==5.1.2
django-ratelimit==4.1.0
pillow==10.4.0
tzdata==2024.2
flake8==7.1.1
