# Training / notebook dependencies (main.ipynb, retrain_oov_artifacts.py).
# Includes runtime deps so a single install covers retrain + serve.
#
#   pip install -r requirements-train.txt

-r requirements.txt

pandas==3.0.5
scikit-learn==1.9.0
